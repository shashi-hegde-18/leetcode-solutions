class Solution:
    def thirdMax(self, nums):
        unique = list(set(nums))
        unique.sort(reverse=True)

        if len(unique) >= 3:
            return unique[2]
        else:
            return unique[0]